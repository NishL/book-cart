<!DOCTYPE html>
<html>
  <head>
    <title>Book Depot</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class='<%= controller.controller_name %>'>
    <div id="banner">
      <%= image_tag 'logo.svg', alt: 'The Awsome Bookshelf' %>
      <span class="title"><%= @page_title %></span>
    </div>
    <div id="columns">
      <div id="side">
        <% if @cart %><!--If @cart is set for any controller then show the cart in the sidebar. Without this, tests would fail & we would get errors visiting another page where @cart is not set in its controller.-->
          <%= hidden_div_if(@cart.line_items.empty?, id: 'cart') do %><!--Use hidden_div_if() helper method, div will have an id of cart if the condition is not met-->
            <%= render @cart %><!--We're invoking the store layout while looking at the index action.-->
          <% end %>
        <% end %>
        <ul>
          <li><%= link_to 'Home', store_index_path %></li>
          <li><a href="#">Questions</a></li>
          <li><a href="#">News</a></li>
          <li><a href="#">Contact</a></li>
        </ul>

        <% if session[:user_id] %>
          <ul>
            <li><%= link_to 'Orders', orders_path %></li>
            <li><%= link_to 'Products', products_path %></li>
            <li><%= link_to 'Users', users_path %></li>
          </ul>
          <%= button_to 'Logout', logout_path, method: :delete %>
        <% end %>
      </div>
      <div id="main">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
